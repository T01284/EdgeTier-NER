name: full_toy
dataset_config: configs/dataset/toy.yaml
model_config: configs/model/char_cnn_crf_small.yaml
train:
  epochs: 5
  batch_size: 4
  lr: 0.001
  weight_decay: 0.0001
  max_len: 64
  device: cpu
  seed: 42
  output_dir: outputs/runs/toy_full
